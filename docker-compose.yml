services:
  app:
    build: .
    working_dir: /app
    command: php -S 0.0.0.0:8080 -t public
    ports:
      - "8088:8080"
    extra_hosts:
      - "SRV-SQL-02:10.61.10.16"
    volumes:
      - .:/app
